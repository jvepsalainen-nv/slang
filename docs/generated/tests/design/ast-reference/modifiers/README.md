---
generated: true
model: claude-opus-4-8[1m]
generated_at: 2026-06-11T11:04:52+00:00
source_commit: 6e923e2c0fe3cae4e7cf40e25a96569df5b9f009
watched_paths_digest: 0da77494c7202daa683caa86dd59c3c52ee670047c4ae4266818e2cd8b88bf99
source_doc: docs/generated/design/ast-reference/modifiers.md
source_doc_digest: ce606e060d178f7091bd26e74fee49fd1d2442061f54a0d3e8579cc174038e5e
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for ast-reference/modifiers

## Intent

Tests verify the user-observable roles of concrete `Modifier` and
`Attribute` subclasses described in
[`docs/generated/design/ast-reference/modifiers.md`](../../../design/ast-reference/modifiers.md):
parameter direction (`in`/`out`/`inout`), storage class (`const`,
`static`, `groupshared`), visibility (`public`/`private`),
interpolation modes (`nointerpolation`, `sample`), matrix layout
(`row_major`), HLSL semantics (`: register(...)`), stage-specific
entry-point attributes (`[shader(...)]`, `[numthreads(...)]`),
compile-time hint attributes (`[unroll]`, `[ForceInline]`),
mutability (`[mutating]`), inheritance control (`[open]`),
differentiability (`[Differentiable]`, `no_diff`).

The doc enumerates a very large family of modifier/attribute classes.
Most carry no user-spellable surface — they are internal checker
flags, core-module bindings, or per-target intrinsic bindings. This
bundle picks the kinds whose role is naturally observable through
`slangc` text I/O at parse, check, or emit, and writes one small
test per role. The bundle is intentionally medium-sized (19 tests)
to span the major families without exhausting every concrete
subclass.

A mix of functional and negative tests is included: emit-text
`SIMPLE(filecheck=...)` directives for modifier spellings that
survive into HLSL/GLSL; `INTERPRET` directives for direction /
storage / mutability semantics that produce a runtime-observable
value; `DIAGNOSTIC_TEST` directives for misuse claims (`const`
assignment, `private` access from outside, non-mutating `this`
assignment, `inout` of a literal).

## Functional coverage

| Claim                                                                                                                                          | Intent     | Anchor                                                                                                                                                                   | Tests                                                                                          |
| ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| A function-scope `static` variable retains its value across calls to the function.                                                             | functional | [#compatibility-and-hlsl-storage-class-modifiers](../../../design/ast-reference/modifiers.md#compatibility-and-hlsl-storage-class-modifiers)                             | [`static-local-persists-across-calls.slang`](static-local-persists-across-calls.slang)         |
| The `groupshared` storage modifier survives lowering to emitted HLSL.                                                                          | functional, expansion | [#compatibility-and-hlsl-storage-class-modifiers](../../../design/ast-reference/modifiers.md#compatibility-and-hlsl-storage-class-modifiers)                             | [`groupshared-emits-on-hlsl.slang`](groupshared-emits-on-hlsl.slang), [`groupshared-fanout-on-glsl.slang`](groupshared-fanout-on-glsl.slang), [`groupshared-fanout-on-metal.slang`](groupshared-fanout-on-metal.slang), [`groupshared-fanout-on-spirv.slang`](groupshared-fanout-on-spirv.slang), [`groupshared-fanout-on-wgsl.slang`](groupshared-fanout-on-wgsl.slang)                           |
| `[ForceInline]` on a small helper inlines it into the caller; the helper's name no longer appears in emitted HLSL.                             | functional, expansion | [#compile-time-hint-attributes-loops-branches-opt-levels](../../../design/ast-reference/modifiers.md#compile-time-hint-attributes-loops-branches-opt-levels)             | [`force-inline-on-hlsl.slang`](force-inline-on-hlsl.slang), [`force-inline-fanout-on-glsl.slang`](force-inline-fanout-on-glsl.slang), [`force-inline-fanout-on-metal.slang`](force-inline-fanout-on-metal.slang), [`force-inline-fanout-on-spirv.slang`](force-inline-fanout-on-spirv.slang), [`force-inline-fanout-on-wgsl.slang`](force-inline-fanout-on-wgsl.slang)                                     |
| `[unroll]` on a loop appears as an `[unroll]` attribute in emitted HLSL.                                                                       | functional, expansion | [#compile-time-hint-attributes-loops-branches-opt-levels](../../../design/ast-reference/modifiers.md#compile-time-hint-attributes-loops-branches-opt-levels)             | [`unroll-attribute-on-hlsl.slang`](unroll-attribute-on-hlsl.slang), [`unroll-attribute-fanout-on-glsl.slang`](unroll-attribute-fanout-on-glsl.slang), [`unroll-attribute-fanout-on-spirv.slang`](unroll-attribute-fanout-on-spirv.slang)                             |
| `CudaKernelAttribute` (`[CudaKernel]`) marks a function as a CUDA kernel; the emitted CUDA carries the `__global__` decoration.                | functional | [#cuda-python-ffi-attributes](../../../design/ast-reference/modifiers.md#cuda-python-ffi-attributes)                                                                     | [`cuda-kernel-emits-global-decoration.slang`](cuda-kernel-emits-global-decoration.slang)       |
| A `[Differentiable]` function can be passed to `fwd_diff` and produces a sensible derivative.                                                  | functional | [#differentiability-attributes](../../../design/ast-reference/modifiers.md#differentiability-attributes)                                                                 | [`differentiable-allows-fwd-diff.slang`](differentiable-allows-fwd-diff.slang)                 |
| `: register(...)` binding semantic propagates to emitted HLSL.                                                                                 | functional, expansion | [#hlsl-semantics-sv](../../../design/ast-reference/modifiers.md#hlsl-semantics-sv)                                                                                       | [`register-semantic-emits-on-hlsl.slang`](register-semantic-emits-on-hlsl.slang), [`register-semantic-fanout-on-glsl.slang`](register-semantic-fanout-on-glsl.slang), [`register-semantic-fanout-on-metal.slang`](register-semantic-fanout-on-metal.slang), [`register-semantic-fanout-on-spirv.slang`](register-semantic-fanout-on-spirv.slang), [`register-semantic-fanout-on-wgsl.slang`](register-semantic-fanout-on-wgsl.slang)               |
| `RayPayloadReadSemantic` / `RayPayloadWriteSemantic` access qualifiers on a `[raypayload]` struct field survive to emitted HLSL.               | functional | [#hlsl-semantics-sv](../../../design/ast-reference/modifiers.md#hlsl-semantics-sv)                                                                                       | [`raypayload-read-semantic-emits-on-hlsl.slang`](raypayload-read-semantic-emits-on-hlsl.slang) |
| An `[open]` interface accepts implementations; a basic implementation works through dynamic dispatch.                                          | functional | [#inheritance-control-attributes](../../../design/ast-reference/modifiers.md#inheritance-control-attributes)                                                             | [`sealed-interface-rejects-inheritance.slang`](sealed-interface-rejects-inheritance.slang)     |
| `nointerpolation` on a pixel-shader varying input survives into emitted HLSL.                                                                  | functional, expansion | [#interpolation-modes](../../../design/ast-reference/modifiers.md#interpolation-modes)                                                                                   | [`nointerpolation-emits-on-hlsl.slang`](nointerpolation-emits-on-hlsl.slang), [`nointerpolation-fanout-on-glsl.slang`](nointerpolation-fanout-on-glsl.slang), [`nointerpolation-fanout-on-metal.slang`](nointerpolation-fanout-on-metal.slang), [`nointerpolation-fanout-on-spirv.slang`](nointerpolation-fanout-on-spirv.slang), [`nointerpolation-fanout-on-wgsl.slang`](nointerpolation-fanout-on-wgsl.slang)                   |
| `sample` interpolation modifier is preserved through emitted HLSL.                                                                             | functional, expansion | [#interpolation-modes](../../../design/ast-reference/modifiers.md#interpolation-modes)                                                                                   | [`sample-interpolation-emits-on-hlsl.slang`](sample-interpolation-emits-on-hlsl.slang), [`sample-interpolation-fanout-on-glsl.slang`](sample-interpolation-fanout-on-glsl.slang), [`sample-interpolation-fanout-on-metal.slang`](sample-interpolation-fanout-on-metal.slang), [`sample-interpolation-fanout-on-spirv.slang`](sample-interpolation-fanout-on-spirv.slang)         |
| `row_major` matrix-layout modifier survives into emitted GLSL layout.                                                                          | functional | [#matrix-layout-modifiers](../../../design/ast-reference/modifiers.md#matrix-layout-modifiers)                                                                           | [`row-major-emits-on-hlsl.slang`](row-major-emits-on-hlsl.slang)                               |
| A non-`[mutating]` struct method cannot assign through `this`; the checker diagnoses it.                                                       | negative   | [#mutability-autodiff-annotations](../../../design/ast-reference/modifiers.md#mutability-autodiff-annotations)                                                           | [`nonmutating-rejects-this-assignment.slang`](nonmutating-rejects-this-assignment.slang)       |
| `[mutating]` on a method allows assigning to fields through `this`; the mutation is visible after the call.                                    | functional | [#mutability-autodiff-annotations](../../../design/ast-reference/modifiers.md#mutability-autodiff-annotations)                                                           | [`mutating-allows-this-assignment.slang`](mutating-allows-this-assignment.slang)               |
| A function without `[ForceInline]` survives as a callable in emitted HLSL.                                                                     | functional, expansion | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers)                           | [`extern-link-time-decl.slang`](extern-link-time-decl.slang), [`noinline-fanout-on-glsl.slang`](noinline-fanout-on-glsl.slang), [`noinline-fanout-on-metal.slang`](noinline-fanout-on-metal.slang), [`noinline-fanout-on-wgsl.slang`](noinline-fanout-on-wgsl.slang)                                   |
| An `in` parameter is a copy; mutation inside the callee does not propagate to the caller.                                                      | functional | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers)                           | [`param-in-default.slang`](param-in-default.slang)                                             |
| An `inout` parameter both reads the caller's value and writes back to it.                                                                      | functional | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers)                           | [`param-inout-reads-and-writes.slang`](param-inout-reads-and-writes.slang)                     |
| An `inout` parameter requires an l-value argument; passing a literal is diagnosed.                                                             | negative   | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers)                           | [`inout-rejects-rvalue.slang`](inout-rejects-rvalue.slang)                                     |
| An `out` parameter writes a value back to the caller's variable.                                                                               | functional | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers)                           | [`param-out-writes-back.slang`](param-out-writes-back.slang)                                   |
| `const` makes a local variable immutable; assigning to it is diagnosed.                                                                        | negative   | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers)                           | [`const-rejects-assignment.slang`](const-rejects-assignment.slang)                             |
| `VulkanRayPayloadAttribute` (`[vk::ray_payload]`) on a closesthit payload propagates to emitted GLSL as a `rayPayloadInEXT`-class declaration. | functional, expansion | [#ray-tracing-attributes](../../../design/ast-reference/modifiers.md#ray-tracing-attributes)                                                                             | [`vk-ray-payload-emits-on-glsl.slang`](vk-ray-payload-emits-on-glsl.slang), [`vk-ray-payload-fanout-on-spirv.slang`](vk-ray-payload-fanout-on-spirv.slang)                     |
| `EarlyDepthStencilAttribute` (`[earlydepthstencil]`) on a fragment entry point survives to emitted HLSL.                                       | functional, expansion | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes)                                               | [`earlydepthstencil-emits-on-hlsl.slang`](earlydepthstencil-emits-on-hlsl.slang), [`earlydepthstencil-fanout-on-glsl.slang`](earlydepthstencil-fanout-on-glsl.slang), [`earlydepthstencil-fanout-on-metal.slang`](earlydepthstencil-fanout-on-metal.slang), [`earlydepthstencil-fanout-on-spirv.slang`](earlydepthstencil-fanout-on-spirv.slang)               |
| `MaxVertexCountAttribute` (`[maxvertexcount(N)]`) on a geometry entry point appears in emitted HLSL with the documented count.                 | functional, expansion | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes)                                               | [`maxvertexcount-emits-on-hlsl.slang`](maxvertexcount-emits-on-hlsl.slang), [`maxvertexcount-fanout-on-glsl.slang`](maxvertexcount-fanout-on-glsl.slang), [`maxvertexcount-fanout-on-spirv.slang`](maxvertexcount-fanout-on-spirv.slang)                     |
| `[numthreads(x,y,z)]` lowers to GLSL `layout(local_size_x=..., local_size_y=..., local_size_z=...)`.                                           | functional, expansion | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes)                                               | [`numthreads-emits-on-glsl.slang`](numthreads-emits-on-glsl.slang), [`numthreads-glsl-claim-fanout-on-hlsl.slang`](numthreads-glsl-claim-fanout-on-hlsl.slang), [`numthreads-glsl-claim-fanout-on-spirv.slang`](numthreads-glsl-claim-fanout-on-spirv.slang), [`numthreads-glsl-claim-fanout-on-wgsl.slang`](numthreads-glsl-claim-fanout-on-wgsl.slang)                             |
| `[numthreads(x,y,z)]` workgroup dimensions appear in emitted HLSL.                                                                             | functional, expansion | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes)                                               | [`numthreads-emits-on-hlsl.slang`](numthreads-emits-on-hlsl.slang), [`numthreads-fanout-on-glsl.slang`](numthreads-fanout-on-glsl.slang), [`numthreads-fanout-on-spirv.slang`](numthreads-fanout-on-spirv.slang), [`numthreads-fanout-on-wgsl.slang`](numthreads-fanout-on-wgsl.slang)                             |
| `[shader("compute")]` marks a function as a compute entry point selectable without -entry.                                                     | functional, expansion | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes)                                               | [`shader-entrypoint-attribute.slang`](shader-entrypoint-attribute.slang), [`shader-entrypoint-fanout-on-glsl.slang`](shader-entrypoint-fanout-on-glsl.slang), [`shader-entrypoint-fanout-on-metal.slang`](shader-entrypoint-fanout-on-metal.slang), [`shader-entrypoint-fanout-on-spirv.slang`](shader-entrypoint-fanout-on-spirv.slang), [`shader-entrypoint-fanout-on-wgsl.slang`](shader-entrypoint-fanout-on-wgsl.slang)                       |
| `no_diff` type modifier suppresses differentiation through a parameter.                                                                        | functional | [#type-modifiers-wrapping-the-type-rather-than-the-declaration](../../../design/ast-reference/modifiers.md#type-modifiers-wrapping-the-type-rather-than-the-declaration) | [`no-diff-suppresses-derivative.slang`](no-diff-suppresses-derivative.slang)                   |
| A `private` member is not accessible from outside its enclosing type.                                                                          | negative   | [#visibility-modifiers](../../../design/ast-reference/modifiers.md#visibility-modifiers)                                                                                 | [`private-rejects-outside-access.slang`](private-rejects-outside-access.slang)                 |
| A `public` member is accessible from outside its enclosing type and a `public` function from anywhere.                                         | functional | [#visibility-modifiers](../../../design/ast-reference/modifiers.md#visibility-modifiers)                                                                                 | [`public-allows-cross-module-access.slang`](public-allows-cross-module-access.slang)           |

## Untested claims

| Claim                                                                                                                                                                                                                                                                                        | Reason                 | Anchor                                                                                               | Why untested                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `[numthreads(x,y,z)]` workgroup dimensions appear in emitted HLSL.                                                                                                                                                                                                                          | unsupported-on-target  | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | The metal target does not emit the workgroup dimensions into source text — the threadgroup size is supplied by the host dispatch API, so the emitted `[[kernel]]` Metal carries no `numthreads`-equivalent token to pin. |
| `sample` interpolation modifier is preserved through emitted HLSL.                                                                                                                                                                                                                          | unsupported-on-target  | [#interpolation-modes](../../../design/ast-reference/modifiers.md#interpolation-modes)               | The wgsl target emits the varying field with `@location(...)` but no per-sample interpolation/sampling qualifier, so there is no `sample`-equivalent token to pin in emitted WGSL.                                       |
| `[unroll]` on a loop appears as an `[unroll]` attribute in emitted HLSL.                                                                                                                                                                                                                    | unsupported-on-target  | [#compile-time-hint-attributes-loops-branches-opt-levels](../../../design/ast-reference/modifiers.md#compile-time-hint-attributes-loops-branches-opt-levels) | Neither the metal nor the wgsl source language has a loop-unroll annotation; the emitted Metal and WGSL render the loop as a plain `for`/`loop` with no unroll-equivalent token to pin.                                  |
| `EarlyDepthStencilAttribute` (`[earlydepthstencil]`) on a fragment entry point survives to emitted HLSL.                                                                                                                                                                                    | unsupported-on-target  | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | The wgsl target has no source-level early-depth-stencil / early-fragment-tests construct; the emitted WGSL `@fragment` entry point carries no early-depth-stencil-equivalent token to pin (HLSL/GLSL/SPIR-V/Metal all carry one). |
| `MaxVertexCountAttribute` (`[maxvertexcount(N)]`) on a geometry entry point appears in emitted HLSL with the documented count.                                                                                                                                                              | unsupported-on-target  | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | The metal and wgsl targets reject the geometry-stage entry point with E36107 (geometry shaders / `TriangleStream.Append` are unavailable in those compilation targets), so there is no emitted text in which to pin the count. |
| `[numthreads(x,y,z)]` lowers to GLSL `layout(local_size_x=..., local_size_y=..., local_size_z=...)`.                                                                                                                                                                                        | unsupported-on-target  | [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | The metal target does not emit the workgroup dimensions into source text — the threadgroup size is supplied by the host dispatch API, so the emitted `[[kernel]]` Metal carries no `numthreads`-equivalent token to pin. |
| A function without `[ForceInline]` survives as a callable in emitted HLSL.                                                                                                                                                                                                                  | unsupported-on-target  | [#parameter-direction-and-storage-class-modifiers](../../../design/ast-reference/modifiers.md#parameter-direction-and-storage-class-modifiers) | The spirv-asm backend inlines all user functions during legalization, so no distinct callable survives in the emitted SPIR-V — the module contains only the entry-point `OpFunction`, leaving no helper symbol to pin. |
| `RayPayloadReadSemantic` / `RayPayloadWriteSemantic` access qualifiers on a `[raypayload]` struct field survive to emitted HLSL.                                                                                                                                                            | unsupported-on-target  | [#hlsl-semantics-sv](../../../design/ast-reference/modifiers.md#hlsl-semantics-sv) | The `: read(...)` / `: write(...)` payload access qualifiers are an HLSL-only source construct. glsl and spirv-asm lower the payload to `rayPayloadInEXT` / `IncomingRayPayloadKHR` but emit no read/write access token; metal and wgsl reject the closesthit entry point with E36107. No non-HLSL target has a read/write-access-qualifier-equivalent token to pin. |
| `row_major` matrix-layout modifier survives into emitted GLSL layout.                                                                                                                                                                                                                       | unsupported-on-target  | [#matrix-layout-modifiers](../../../design/ast-reference/modifiers.md#matrix-layout-modifiers) | Absent targets: hlsl metal wgsl spirv-asm. hlsl / metal / wgsl emit the matrix through a transposed `_MatrixStorage_*` struct with no `row_major`-equivalent token; spirv-asm emits `ColMajor` (Slang's column-major storage convention with transposed access), not a row-major marker. None of these four targets carries a faithful `row_major`-survival token to pin. |
| `VulkanRayPayloadAttribute` (`[vk::ray_payload]`) on a closesthit payload propagates to emitted GLSL as a `rayPayloadInEXT`-class declaration.                                                                                                                                              | unsupported-on-target  | [#ray-tracing-attributes](../../../design/ast-reference/modifiers.md#ray-tracing-attributes) | The Vulkan ray-payload storage class has no HLSL equivalent — hlsl emits the payload as an ordinary `inout` closesthit parameter, not a storage-class declaration; metal and wgsl reject the closesthit entry point with E36107. (glsl and spirv-asm — `rayPayloadInEXT` / `IncomingRayPayloadKHR` — are covered.) |
| Python / FFI attributes (`[TorchEntryPoint]`, `[PyExport]`, `[DllImport]`, `[AutoPyBindCuda]`) ship through a host Python / PyTorch / C ABI runtime that the agent runtime does not include, and that the `slang-test` harness does not invoke.                                              | requires-external-tool | [#cuda-python-ffi-attributes](../../../design/ast-reference/modifiers.md#cuda-python-ffi-attributes) | Verifying these attributes' behavior requires running a Python / PyTorch / loader runtime against the emitted artifact; no `slang-test` directive surfaces that. |
| GLSL `layout(...)` family (`[vk::binding(...)]`, `[vk::location(...)]`, `[push_constant]`, etc.) when the role is best observed via SPIR-V validation on a real Vulkan driver. Emit-text observations for these are partially covered by sibling bundles; we omit duplicates here.           | (unclassified)         | (unspecified)                                                                                        | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| Parent class in the C++ hierarchy (`InOutModifier` derives from `OutModifier`; `Attribute` derives from `AttributeBase` derives from `Modifier`; `HLSLRegisterSemantic` derives from `HLSLLayoutSemantic` derives from `HLSLSemantic`).                                                      | (unclassified)         | [#inoutmodifier](../../../design/ast-reference/modifiers.md#inoutmodifier)                           | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| Core-module / target binding modifiers (`IntrinsicOpModifier`, `TargetIntrinsicModifier`, `SpecializedForTargetModifier`, `BuiltinTypeModifier`, `MagicTypeModifier`, `BuiltinAttribute`, `AutoDiffBuiltinAttribute`, `KnownBuiltinAttribute`) — not user-spellable outside the core module. | (unclassified)         | [#intrinsicopmodifier](../../../design/ast-reference/modifiers.md#intrinsicopmodifier)               | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| `MemoryQualifierSetModifier` bitmask aggregation is a checker- internal representation; user observation is the resulting GLSL emit (where individual qualifiers appear).                                                                                                                    | (unclassified)         | [#memoryqualifiersetmodifier](../../../design/ast-reference/modifiers.md#memoryqualifiersetmodifier) | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| Internal-only modifiers (`ToBeSynthesizedModifier`, `SynthesizedModifier`, `IgnoreForLookupModifier`, `VarReassignedModifier`, `ExistentialOpenedOnVarModifier`, `LocalTempVarModifier`, `ActualGlobalModifier`, `IsOverridingModifier`, `OptionalConstraintModifier`).                      | (unclassified)         | [#tobesynthesizedmodifier](../../../design/ast-reference/modifiers.md#tobesynthesizedmodifier)       | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| `UncheckedAttribute` is the parser-time shape before checker resolves it; user-visible behavior is the resolved attribute.                                                                                                                                                                   | (unclassified)         | [#uncheckedattribute](../../../design/ast-reference/modifiers.md#uncheckedattribute)                 | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| Abstract intermediates that carry no user spelling (`VisibilityModifier`, `InterpolationModeModifier`, `MatrixLayoutModifier`, `HLSLSemantic`, `TypeModifier`, `AttributeBase`, `InheritanceControlAttribute`, `RayPayloadAccessSemantic`).                                                  | (unclassified)         | [#visibilitymodifier](../../../design/ast-reference/modifiers.md#visibilitymodifier)                 | Reason and explanation to be refined by the next regeneration.                                                                                                   |
| C++ class identity of the parser-allocated modifier (e.g. that `groupshared` becomes `HLSLGroupSharedModifier` specifically and not a synonym; that `[unroll]` becomes `UnrollAttribute`).                                                                                                   | needs-unit-test        | [#groupshared](../../../design/ast-reference/modifiers.md#groupshared)                               | No slangc CLI surface reaches this. A C++ unit test in `tools/slang-unit-test/` could exercise the relevant compiler internals directly.                         |
| The mermaid "Family hierarchy" graph as a graph — the topology is structural metadata about the class hierarchy, not user behavior.                                                                                                                                                          | internal-source-fact   | [#family-hierarchy](../../../design/ast-reference/modifiers.md#family-hierarchy)                     | Implementation detail (C++ class hierarchy, field names, parser-callback names) with no user-observable consequence.                                             |
| Private/key field names and types (e.g. the `irOp: uint32_t` field on `IntrinsicOpModifier`, the bitmask on `MemoryQualifierSetModifier`, the version `Token` on `GLSLVersionDirective`).                                                                                                    | internal-source-fact   | [#intrinsicopmodifier](../../../design/ast-reference/modifiers.md#intrinsicopmodifier)               | Implementation detail (C++ class hierarchy, field names, parser-callback names) with no user-observable consequence.                                             |

## Doc gaps observed

| Anchor                                                                                                                   | Kind                  | Gap                                                                                                                                                                                                                                                                                                                                                | Suggested addition |
| ------------------------------------------------------------------------------------------------------------------------ | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| [#hlslnointerpolationmodifier](../../../design/ast-reference/modifiers.md#hlslnointerpolationmodifier)                   | undocumented-behavior | The doc enumerates `HLSLNoInterpolationModifier`, `HLSLNoPerspectiveModifier`, `HLSLLinearModifier`, `HLSLSampleModifier`, `HLSLCentroidModifier`, `PerVertexModifier` but does not specify which combinations are mutually exclusive. A reader cannot tell from the doc whether `nointerpolation sample` on the same field is valid or diagnosed. |                    |
| [#interpolation-modes](../../../design/ast-reference/modifiers.md#interpolation-modes)                                   | missing-example       | The doc names the interpolation-mode modifiers generically but does not enumerate, per shader target, how each lowers (or that it cannot). Observed: `nointerpolation` reaches every shader target (`flat` / `[[flat]]` / `Flat` / `@interpolate(flat)`), but `sample` has no per-sample qualifier in emitted WGSL — that target silently drops it. | Add a per-target lowering column to the interpolation-modes table (e.g. `nointerpolation` → GLSL `flat`, Metal `[[flat]]`, SPIR-V `Flat`, WGSL `@interpolate(flat)`; `sample` → GLSL `sample`, Metal `[[sample_perspective]]`, SPIR-V `Sample`, WGSL: not expressed). |
| [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | missing-example       | The doc names `[numthreads(x,y,z)]` but does not state that the Metal backend does not emit the workgroup dimensions into source text (they are supplied by the host dispatch API), unlike HLSL/GLSL/SPIR-V/WGSL which all carry the chosen dimensions in emitted text. | Add a per-target note to the `NumThreadsAttribute` row recording that Metal omits the dimensions from emitted source (host-API-supplied) while HLSL/GLSL/SPIR-V/WGSL embed them. |
| [#compile-time-hint-attributes-loops-branches-opt-levels](../../../design/ast-reference/modifiers.md#compile-time-hint-attributes-loops-branches-opt-levels) | missing-example       | The doc names `[unroll]` (`UnrollAttribute`) but does not state that only HLSL/GLSL/SPIR-V carry an unroll marker into emitted text (`[unroll]` / `[[unroll]]` / `OpLoopMerge ... Unroll`); Metal and WGSL have no source-level unroll annotation and emit a plain loop. | Add a per-target note to the `UnrollAttribute` row recording that Metal and WGSL emit the loop without an unroll-equivalent token, while HLSL/GLSL/SPIR-V preserve the hint. |
| [#internal-synthesized-modifiers](../../../design/ast-reference/modifiers.md#internal-synthesized-modifiers)             | undocumented-behavior | The "Internal / synthesized modifiers" subsection lists modifiers but the doc does not state from which checker pass each one is introduced. A reader cannot tell whether `IgnoreForLookupModifier` is set at parse, check, or IR-lowering time.                                                                                                   |                    |
| [#forceinline](../../../design/ast-reference/modifiers.md#forceinline)                                                   | undocumented-behavior | `[ForceInline]` is documented as `ForceInlineAttribute` but the doc does not state whether the helper function survives in emitted text (helpful guarantee for downstream tooling). The bundle's `force-inline-on-hlsl.slang` documents the observed behavior but the claim itself is not in the doc.                                              |                    |
| [#openattribute](../../../design/ast-reference/modifiers.md#openattribute)                                               | undocumented-behavior | The doc mentions `OpenAttribute` and `SealedAttribute` under `InheritanceControlAttribute` but does not specify the default for an interface that carries neither — i.e. whether a bare `interface` is implicitly open or sealed.                                                                                                                  |                    |
| [#modern-language-defaults-to-internal](../../../design/ast-reference/modifiers.md#modern-language-defaults-to-internal) | undocumented-behavior | The visibility default rule ("modern language defaults to `internal`") is mentioned only in the "Visibility modifiers and language version" subsection. A reader looking at the `PublicModifier` / `PrivateModifier` / `InternalModifier` row in the main table would not know the default is per-module-language.                                 |                    |
| [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | missing-example | The doc names `[earlydepthstencil]` (`EarlyDepthStencilAttribute`) generically but does not enumerate its per-target lowering. Observed: HLSL `[earlydepthstencil]`, GLSL `layout(early_fragment_tests) in`, SPIR-V `OpExecutionMode EarlyFragmentTests`, Metal `[[early_fragment_tests]]`, but WGSL has no early-fragment-tests construct and drops it silently. | Add a per-target lowering note to the `EarlyDepthStencilAttribute` row enumerating the HLSL/GLSL/SPIR-V/Metal spellings and recording that WGSL does not express it. |
| [#stage-specific-entry-point-attributes](../../../design/ast-reference/modifiers.md#stage-specific-entry-point-attributes) | missing-example | The doc names `[maxvertexcount(N)]` (`MaxVertexCountAttribute`) generically but does not enumerate per target. Observed: HLSL `[maxvertexcount(N)]`, GLSL `layout(max_vertices = N) out`, SPIR-V `OpExecutionMode OutputVertices N`; Metal and WGSL reject geometry-stage entry points entirely (E36107). | Add a per-target note to the `MaxVertexCountAttribute` row enumerating the HLSL/GLSL/SPIR-V spellings and recording that Metal and WGSL do not support the geometry stage. |
| [#hlsl-semantics-sv](../../../design/ast-reference/modifiers.md#hlsl-semantics-sv) | missing-example | The doc names `: register(...)` (`HLSLRegisterSemantic`) generically but does not enumerate how the slot index maps per target. Observed: the `register(u3)` index reaches GLSL `binding = 3`, Metal `[[buffer(3)]]`, SPIR-V `OpDecorate ... Binding 3`, and WGSL `@binding(3)` — not just HLSL `register(u3)`. | Add a per-target note to the `HLSLRegisterSemantic` row recording that the register slot index maps to the binding number on GLSL/SPIR-V/Metal/WGSL. |
| [#hlsl-semantics-sv](../../../design/ast-reference/modifiers.md#hlsl-semantics-sv) | undocumented-behavior | The doc names `RayPayloadReadSemantic` / `RayPayloadWriteSemantic` (`: read(...)` / `: write(...)`) but does not state these access qualifiers are HLSL-only — no non-HLSL target emits a read/write access token. GLSL/SPIR-V lower the payload to `rayPayloadInEXT` / `IncomingRayPayloadKHR` without the qualifiers; Metal/WGSL reject closesthit. | Add a note to the `RayPayloadReadSemantic` / `RayPayloadWriteSemantic` row recording that the access qualifiers survive only into emitted HLSL and have no equivalent in other targets. |
| [#matrix-layout-modifiers](../../../design/ast-reference/modifiers.md#matrix-layout-modifiers) | undocumented-behavior | The doc names `row_major` (`HLSLRowMajorLayoutModifier`) but does not state that only GLSL carries a `row_major` token. Observed: HLSL/Metal/WGSL emit the matrix through a transposed `_MatrixStorage_*` struct with no `row_major` token, and SPIR-V emits `ColMajor` (Slang's column-major storage convention with transposed access), not a row-major marker. | Add a per-target lowering note to the `HLSLRowMajorLayoutModifier` row recording that only GLSL preserves a `row_major` token while other targets realize the layout via a transposed storage struct / `ColMajor` decoration. |
| [#ray-tracing-attributes](../../../design/ast-reference/modifiers.md#ray-tracing-attributes) | missing-example | The doc names `[vk::ray_payload]` (`VulkanRayPayloadAttribute`) generically but does not enumerate per target. Observed: GLSL `rayPayloadInEXT`, SPIR-V `IncomingRayPayloadKHR` storage class; HLSL emits the payload as an ordinary `inout` closesthit parameter (no storage-class declaration); Metal and WGSL reject closesthit (E36107). | Add a per-target note to the `VulkanRayPayloadAttribute` row enumerating the GLSL `rayPayloadInEXT` / SPIR-V `IncomingRayPayloadKHR` lowering and recording HLSL's plain-parameter form plus Metal/WGSL non-support. |
| [#forceinline](../../../design/ast-reference/modifiers.md#forceinline) | undocumented-behavior | The doc does not state, per target, whether a non-`[ForceInline]` helper survives as a distinct callable. Observed: GLSL/Metal/WGSL preserve the user helper as its own function (modulo mangling), but the SPIR-V backend inlines all user functions during legalization, leaving only the entry-point `OpFunction`. | Add a per-target note recording that the spirv-asm backend always inlines user helpers (no distinct callable survives) while GLSL/Metal/WGSL/HLSL preserve them. |
